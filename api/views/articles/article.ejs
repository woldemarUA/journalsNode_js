<%- include('../partials/header'); %>
    <%- include('../partials/nav'); %>
        <div class="container w-50 d-flex">
            <div class="card">
                <div class="card-img-top">

                    <img src="../../<%= article.image %>" class="img-fluid" alt="<%= article.title %>">
                </div>
                <div class="card-body">
                    <h5 class="card-title">
                        <%= article.title %>
                    </h5>
                    <h6 class="card-subtitle mb-2 text-muted">Ecrit par: <%= article.author %>
                    </h6>
                    <p class="card-text">
                        <%= article.description %>
                    </p>
                    <p class="card-text text-muted">
                        Postée par <%= article.User.nom %>
                    </p>
                    <a href="/" class="btn btn-secondary btn-sm">Retour a liste</a>
                    <%- include('../components/buttonsEditDelete') %>
                </div>
            </div>
        </div>
        <%- include('../components/modalDelete' ) %>
            <%- include('../partials/footer'); %>