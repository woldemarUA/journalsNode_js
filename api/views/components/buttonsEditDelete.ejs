<% if(isAuthenticated){ %>

    <a href="/articles/edit?id=<%= article.id %>" class="btn btn-warning btn-sm">Edit</a>
    <button class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#deleteArticleModal"
        data-title="<%= article.title %>" data-id="<%= article.id %>">Effacer</button>



    <% }%>