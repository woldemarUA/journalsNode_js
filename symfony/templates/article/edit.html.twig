{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Edit Article</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Soumettre'}) }}

    <a class="btn btn-primary btn-sm my-1" href="{{ path('app_article_index') }}">back to list</a>
    {% if is_granted('IS_AUTHENTICATED_FULLY') and article.userId.id  is same as (app.user.id)  %}
        {{ include('article/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
